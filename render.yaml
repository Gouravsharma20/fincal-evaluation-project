services:
  - type: static
    name: fincal-evaluation-project
    buildCommand: cd client && npm install && npm run build
    staticPublishPath: client/build
    envVars:
      - key: NODE_ENV
        value: production
      - key: REACT_APP_API_URL
        value: https://crm-management-api-14fg.onrender.com