{
  "version": 2,
  "buildCommand": "cd client && npm run build && mkdir -p ../public && cp -r build/* ../public/",
  "outputDirectory": "public",
  "installCommand": "npm install && cd client && npm install"
}
